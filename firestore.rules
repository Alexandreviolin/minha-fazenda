rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regra inicial fechada (ajustaremos nas funções)
      allow read, write: if request.auth != null;
    }
  }
}
